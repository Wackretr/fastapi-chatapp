# 指示書（BRANQ&A Lite デモ拡張）

## 目的
当事者／支援者が同じ会話履歴を共有しながらデモ面談を体験できる状態をベースに、次フェーズの開発タスクを整理・実行するための手順書です。

## 作業フロー
1. **環境準備**
   - Poetry と Node.js の依存をインストール（`poetry install`, `npm install`）。
   - バックエンド (`poetry run uvicorn ...`) → フロントエンド (`npm run dev`) の順で起動し、`http://localhost:5173/` にアクセス。
2. **デモ確認**
   - 当事者モードでメッセージ送信 → 支援者モードで履歴と返神候補を確認。
   - 候補を「この案を入力」で取り込み、送信して双方のログが更新されることを確認。
3. **テスト実行**
   - `npm test -- --run` を実行。`act` 警告は既知のため、ログに注意しつつ改善予定として扱う。
4. **開発タスク（推奨順）**
   1. Vitest の `act` 警告を解消（`await act(async () => user.click(...))` 等でラップ）。
   2. 会話履歴を FastAPI 側で永続化し、Context から API 経由で取得／投稿できるようにする。
   3. 入力中インジケータやメッセージ削除など、要件一覧にある機能を段階的に実装。
   4. 支援者検索・設定を実データと連携。ユーザー検索 API の設計を並行して行う。
   5. 返神使用回数の管理と面談評価機能を追加。評価 1 回ごとに利用上限が増える仕様をバックエンドで管理する。
   6. 管理者向けログ閲覧・AI 応答評価 UI のモックを作り、要件確認後に実 API を実装。
5. **ドキュメント更新**
   - 新規 API 追加や仕様変更があれば `HANDOVER.md` とこの指示書を更新する。
   - 必要に応じて `README.md` に起動・環境構築手順を追記。

## チェックリスト
- [ ] テスト (`npm test -- --run`) が通る。
- [ ] 手動デモで当事者→支援者の会話連携が確認できる。
- [ ] 返神候補取得エラー時のトースト／メッセージが表示される。
- [ ] 新規 API 実装時に FastAPI 側でリクエストスキーマとレスポンススキーマを Pydantic で定義。
- [ ] セキュリティ（認証・認可）やログ監査の要件を満たすためのタスクをチケット化。

## 補足
- リアルタイム通信を導入する場合は FastAPI の WebSocket かサーバー送信イベント (SSE) を検討。React 側では Context を通した購読に変更する。
- 返神（AI レコメンド）ロジックを本番想定に近付ける際は、回答案と採用結果を蓄積し、評価フィードバックをモデル改善に活用する仕組みを検討する。
- 実運用では当事者／支援者アプリを別デプロイにする想定のため、ルーティングやビルド設定を将来的に分離できるようコンポーネントをモジュール化しておく。

以上。*** End Patch
